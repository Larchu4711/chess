{
  "name": "chess",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Schach im Browser mit selbst gestalteten Figuren",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
